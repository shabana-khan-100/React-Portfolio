# Active Context: Personal Portfolio

## 1. Current Focus

The current focus is on redesigning the entire portfolio UI to match the aesthetic of a reference portfolio. This involves a complete overhaul of the styling, layout, and components.

## 2. Next Steps

The following steps will be taken to implement the new design:

1.  **Update Memento Documentation**: Align all Memento files with the new project direction.
2.  **Global Styling**:
    - Implement a dark theme and custom cursor in `src/index.css`.
3.  **Component Redesign/Creation**:
    - Refactor `App.js` for a single-page scroll layout.
    - Redesign `Hero.js`.
    - Redesign `About.js`.
    - Create a new `Timeline.js` component for Experience, Education, and Certifications.
    - Create a new `TechSet.js` component to replace `Skills.js` and `SkillsChart.js`.
4.  **Component Removal**:
    - Remove `Header.js`, `Sidebar.js`, `Skills.js`, and `SkillsChart.js`.
5.  **Projects Section**:
    - The user has not yet specified whether to keep the "Projects" section. This will be addressed later.
